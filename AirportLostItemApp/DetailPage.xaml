<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AirportLostItemApp.DetailPage"
             Title="Eşya Detayı"
             BackgroundColor="#F0F2F5">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">

            <Frame BackgroundColor="White" CornerRadius="20" HasShadow="True" Padding="20" BorderColor="Transparent">
                <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="Auto, *">
                    
                    <Frame Grid.RowSpan="3" Grid.Column="0" 
                           HeightRequest="90" WidthRequest="90" 
                           CornerRadius="45" 
                           Padding="0" 
                           IsClippedToBounds="True" 
                           BorderColor="#E0E0E0"
                           Margin="0,0,15,0">
                        <Image Source="{Binding ImageUrl}" Aspect="AspectFill" HorizontalOptions="Fill" VerticalOptions="Fill"/>
                    </Frame>
                    
                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding Name}" FontSize="22" FontAttributes="Bold" TextColor="#1565C0" VerticalOptions="End"/>
                    
                    <Label Grid.Row="1" Grid.Column="1" Text="{Binding Location}" FontSize="14" TextColor="Gray"/>
                    
                    <Frame Grid.Row="2" Grid.Column="1" BackgroundColor="{Binding StatusColor}" Padding="8,4" HorizontalOptions="Start" CornerRadius="8" Margin="0,5,0,0" HasShadow="False">
                         <Label Text="{Binding Status}" TextColor="White" FontSize="11" FontAttributes="Bold"/>
                    </Frame>
                </Grid>
            </Frame>

            <VerticalStackLayout Spacing="15">
                <VerticalStackLayout Spacing="5">
                    <Label Text="📍 Kaybolduğu Yer" FontAttributes="Bold" TextColor="#1565C0" Margin="5,0,0,0"/>
                    <Frame Padding="15" CornerRadius="12" BackgroundColor="White" BorderColor="Transparent" HasShadow="False">
                        <Label Text="{Binding Location}" TextColor="#333" FontSize="15"/>
                    </Frame>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="5">
                    <Label Text="📅 Kayıt Tarihi" FontAttributes="Bold" TextColor="#1565C0" Margin="5,0,0,0"/>
                    <Frame Padding="15" CornerRadius="12" BackgroundColor="White" BorderColor="Transparent" HasShadow="False">
                        <Label Text="{Binding Date}" TextColor="#333" FontSize="15"/>
                    </Frame>
                </VerticalStackLayout>
            </VerticalStackLayout>

            <Frame BackgroundColor="#E8F5E9" CornerRadius="20" Padding="20" Margin="0,10,0,0" BorderColor="#4CAF50" HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    <Label Text="👋 Bu Eşyayı Buldunuz mu?" TextColor="#2E7D32" FontAttributes="Bold" FontSize="18" HorizontalOptions="Center"/>
                    <Label Text="Lütfen eşyayı kime teslim ettiğinizi yazın:" TextColor="#1B5E20" FontSize="13" HorizontalOptions="Center"/>

                    <Entry x:Name="DeliveryLocationEntry" 
                           Placeholder="Örn: Güvenlik, Danışma..." 
                           BackgroundColor="White" 
                           TextColor="Black"
                           PlaceholderColor="Gray"
                           HeightRequest="45"/>

                    <Button Text="✅ TESLİM ETTİM" 
                            Clicked="OnFoundClicked"
                            BackgroundColor="#4CAF50" 
                            TextColor="White" 
                            FontAttributes="Bold" 
                            CornerRadius="25" 
                            HeightRequest="50"
                            Margin="0,5,0,0"/>
                </VerticalStackLayout>
            </Frame>

            <Label Text="Bu Eşya Benim (Talep Et)" 
                   TextColor="#1565C0" 
                   FontSize="14"
                   HorizontalOptions="Center"
                   TextDecorations="Underline"
                   Margin="0,10,0,30">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnClaimClicked"/>
                </Label.GestureRecognizers>
            </Label>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>