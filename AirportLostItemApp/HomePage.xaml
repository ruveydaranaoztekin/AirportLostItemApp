<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AirportLostItemApp.HomePage"
             BackgroundColor="#F5F5F5"
             Title="Vitrin">

    <CollectionView x:Name="VitrinList" 
                    SelectionMode="Single"
                    SelectionChanged="OnItemSelected"
                    Margin="0">

        <CollectionView.Header>
            <VerticalStackLayout Spacing="0">
                
                <Frame BackgroundColor="#1565C0" Padding="15" CornerRadius="0" HasShadow="True" Margin="0,0,0,10">
                    <VerticalStackLayout Spacing="10">
                        <Grid ColumnDefinitions="*, Auto">
                            <Label Text="🔎 VİTRİN" TextColor="White" FontAttributes="Bold" VerticalOptions="Center"/>
                            <Label Text="İstanbul Havalimanı" TextColor="#BBDEFB" FontSize="12" HorizontalOptions="End" VerticalOptions="Center"/>
                        </Grid>
                        <SearchBar x:Name="searchBar" Placeholder="Kaybolan eşyayı ara..." BackgroundColor="White" TextChanged="OnSearchBarTextChanged"/>
                    </VerticalStackLayout>
                </Frame>

                <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never" Padding="10" BackgroundColor="Transparent">
                    <HorizontalStackLayout Spacing="10" Margin="0,0,0,10">
                        <Button Text="Tümü" Clicked="OnCategoryClicked" CommandParameter="Tümü" BackgroundColor="#1565C0" TextColor="White" HeightRequest="35" CornerRadius="15"/>
                        <Button Text="💻 Elektronik" Clicked="OnCategoryClicked" CommandParameter="Elektronik" BackgroundColor="White" TextColor="#1565C0" HeightRequest="35" CornerRadius="15" BorderColor="#1565C0" BorderWidth="1"/>
                        <Button Text="🧳 Bavul" Clicked="OnCategoryClicked" CommandParameter="Bavul" BackgroundColor="White" TextColor="#1565C0" HeightRequest="35" CornerRadius="15" BorderColor="#1565C0" BorderWidth="1"/>
                        <Button Text="🛂 Pasaport" Clicked="OnCategoryClicked" CommandParameter="Pasaport" BackgroundColor="White" TextColor="#1565C0" HeightRequest="35" CornerRadius="15" BorderColor="#1565C0" BorderWidth="1"/>
                        <Button Text="🧸 Oyuncak" Clicked="OnCategoryClicked" CommandParameter="Oyuncak" BackgroundColor="White" TextColor="#1565C0" HeightRequest="35" CornerRadius="15" BorderColor="#1565C0" BorderWidth="1"/>
                    </HorizontalStackLayout>
                </ScrollView>

                <Label Text="📢 Son Eklenenler" FontAttributes="Bold" Margin="15,5,0,10" TextColor="#424242"/>
            
            </VerticalStackLayout>
        </CollectionView.Header>

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Margin="10,5" CornerRadius="12" Padding="0" IsClippedToBounds="True" BorderColor="Transparent" HasShadow="True">
                    <Grid ColumnDefinitions="80, *, Auto" BackgroundColor="White">
                        <BoxView Grid.Column="0" Color="#E3F2FD"/>
                        <Label Grid.Column="0" Text="{Binding Icon}" FontSize="35" HorizontalOptions="Center" VerticalOptions="Center"/>
                        
                        <VerticalStackLayout Grid.Column="1" Padding="10" VerticalOptions="Center" Spacing="3">
                            <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="16" TextColor="#1F1F1F"/>
                            <HorizontalStackLayout Spacing="5">
                                <Label Text="📍" FontSize="12"/>
                                <Label Text="{Binding Location}" TextColor="Gray" FontSize="12"/>
                            </HorizontalStackLayout>
                            <Label Text="{Binding DateLost}" TextColor="#90CAF9" FontSize="10"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Column="2" Padding="10" VerticalOptions="Center" Spacing="5">
                            <Label Text="{Binding Priority}" TextColor="Red" FontSize="10" FontAttributes="Bold" HorizontalOptions="End"/>
                            <Label Text="➡️" TextColor="LightGray" FontSize="20"/>
                        </VerticalStackLayout>
                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>
</ContentPage>