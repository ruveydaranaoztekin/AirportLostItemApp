<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AirportLostItemApp.DetailPage"
             Title="Eşya Detayı"
             BackgroundColor="#F0F2F5">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">

            <Frame BackgroundColor="White" CornerRadius="20" HasShadow="True" Padding="20">
                <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="Auto, *">
                    <Frame Grid.RowSpan="3" Grid.Column="0" HeightRequest="90" WidthRequest="90" CornerRadius="45" Padding="0" IsClippedToBounds="True" Margin="0,0,15,0">
                        <Image Source="{Binding ImageUrl}" Aspect="AspectFill"/>
                    </Frame>
                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding Name}" FontSize="22" FontAttributes="Bold" TextColor="#1565C0" VerticalOptions="End"/>
                    <Label Grid.Row="1" Grid.Column="1" Text="{Binding Location}" FontSize="14" TextColor="Gray"/>
                    <Frame Grid.Row="2" Grid.Column="1" BackgroundColor="{Binding StatusColor}" Padding="8,4" HorizontalOptions="Start" CornerRadius="8" Margin="0,5,0,0">
                         <Label Text="{Binding Status}" TextColor="White" FontSize="11" FontAttributes="Bold"/>
                    </Frame>
                </Grid>
            </Frame>

            <VerticalStackLayout Spacing="15">
                <Label Text="📋 İlan Detayları" FontAttributes="Bold" TextColor="#333" FontSize="18"/>
                
                <Frame Padding="15" CornerRadius="12" BackgroundColor="White">
                    <VerticalStackLayout Spacing="5">
                         <Label Text="Açıklama / Özellikler:" TextColor="#0056D2" FontSize="12" FontAttributes="Bold"/>
                         <Label Text="{Binding Description}" TextColor="#333" FontSize="14"/>
                    </VerticalStackLayout>
                </Frame>

                <Frame Padding="15" CornerRadius="12" BackgroundColor="White">
                    <VerticalStackLayout Spacing="5">
                         <Label Text="Bulan Kişiye Not:" TextColor="#0056D2" FontSize="12" FontAttributes="Bold"/>
                         <Label Text="{Binding NoteToFinder}" TextColor="#333" FontSize="14" FontAttributes="Italic"/>
                    </VerticalStackLayout>
                </Frame>
                
                <Frame Padding="15" CornerRadius="12" BackgroundColor="White">
                     <Grid ColumnDefinitions="*,*">
                         <VerticalStackLayout>
                             <Label Text="Kaybolduğu Yer" TextColor="Gray" FontSize="11"/>
                             <Label Text="{Binding Location}" TextColor="#333" FontAttributes="Bold"/>
                         </VerticalStackLayout>
                         <VerticalStackLayout Grid.Column="1">
                             <Label Text="Kayıt Tarihi" TextColor="Gray" FontSize="11"/>
                             <Label Text="{Binding Date}" TextColor="#333" FontAttributes="Bold"/>
                         </VerticalStackLayout>
                     </Grid>
                </Frame>
            </VerticalStackLayout>

            <Button Text="Bu Eşya Benim (Talep Et)" 
                    Clicked="OnClaimClicked"
                    BackgroundColor="Transparent" 
                    TextColor="#1565C0" 
                    FontSize="14"
                    Margin="0,10,0,30"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>